html
  meta(name='viewport' content='width=device-width, user-scalable=no')

  block styles
    link(rel='stylesheet' href='docs/vendor.css')
    link(rel='stylesheet' href='dist/mn-backdrop.css')

body
  h2 mn-backdrop
  include github-corner

  .mn-layout-fullscreen.mn-layout-centered
    .mn-layout-container.mn-layout-row.align-center
      button.mn-button show backdrop
  
  .docs.mn-layout-container
    mn-code.sh.
      # to install this module
      npm install --save mn-backdrop

    p And bundle dependencies and main files in <strong>dist/</strong> with your preferred tool.

    h3 usage
    p Use the methods from class <strong>MnBackdrop</strong>
    mn-code.js.
      MnBackdrop.show()

    p and hide
    mn-code.js.
      MnBackdrop.hide()

    h3 Target
    p Sometimes, different dependencies use this backdrop, to avoid conflicts between their, define a target as owner of backdrop, on methods, using the option <i>target</i>, eg.

    mn-code.js.
      MnBackdrop.show({target: element})
      MnBackdrop.hide() // dont work, because backdrop has a target as owner
      MnBackdrop.hide({target: element})

  block scripts
    script(src='docs/vendor.js')
    script(src='dist/mn-backdrop.js')

  script.
    var button = document.querySelector('.mn-button')
    button.addEventListener('click', showBackdrop)
    document.body.addEventListener('click', hideBackdrop)

    function showBackdrop(event) {
      //- MnBackdrop.show({target: event.target})
      MnBackdrop.show()
      event.target.blur()
      event.stopPropagation()
    }

    function hideBackdrop() {
      //- MnBackdrop.hide({target: button})
      MnBackdrop.hide()
    }
